# GLOBAL-SUCCESS-10.md — spec triển khai module bài giảng

Tài liệu thi công cho `Global_Success_10/` trên <https://hsg2627.github.io>.
Dành cho tác nhân deploy (Antigravity và mọi tác nhân khác).

**Đọc hết mục 1 và mục 2 trước khi sửa bất kỳ dòng nào.** Module này cố tình
thiếu ba thứ mà phần còn lại của trang có. Thiếu là đúng, không phải sót.

> **Tệp này tự đủ.** `AGENTS.md` và `SITE-SPEC.md` nằm ở kho tài liệu riêng của
> chủ dự án, **không** có trong repo này. Mọi chỗ dẫn hai tệp đó dưới đây là để
> ghi nguồn gốc quyết định, không phải yêu cầu đi tìm. Đừng dừng lại vì không
> thấy chúng; nội dung cần dùng đã chép đủ vào đây và vào `gs10.css`.

---

## 0. Thứ tự ưu tiên khi tài liệu mâu thuẫn

```
AGENTS.md  >  GLOBAL-SUCCESS-10.md  >  SITE-SPEC.md
```

Với **riêng thư mục `Global_Success_10/`**, tệp này thắng `SITE-SPEC.md`.
Ba mục sau của SITE-SPEC đã lỗi thời và **không được thi hành**:

| SITE-SPEC nói | Thực tế |
|---|---|
| §9.2 *"module đã dựng xong, không dựng lại"* | Chưa từng có. Dựng mới ngày 2026-08-27. |
| §9.2.2 dựng cổng khoá `gs10-gate.js` | **Đã bỏ.** Xem §2.2. |
| dòng 475: *"bài giảng cũng do AI sinh"* → cần banner AI | **Sai.** Nội dung do giáo viên viết. Xem §2.3. |

Chốt ngày 2026-08-27 bởi chủ dự án. Nếu SITE-SPEC được cập nhật sau ngày đó và
mâu thuẫn với tệp này, hỏi lại chứ đừng tự chọn.

### 0.1 Bản dựng theo SITE-SPEC cũ đã được thay

Trong repo từng có một `Global_Success_10/` khác, dựng đúng theo `SITE-SPEC.md`
§9.2 (commit `cb75168` → `a58a30c`, 2026-08-26/27): `js/gs10-gate.js` có màn hình
khoá học sinh và mật khẩu giáo viên, `js/gs10-player.js` nạp nội dung bằng
`fetch('data/unitNN.json')`, `index.html` dùng `<script type="module">`.

Chủ dự án xác nhận đó là **bản thô để dựng cấu trúc dữ liệu**, và chốt thay bằng
bản hiện tại. Ba tệp `css/`, `js/`, `index.html` cũ đã gỡ vì cả ba đều vi phạm
§2.2 và §2.4 — bản cũ không mở được bằng `file://`.

**Nhưng nội dung thì giữ.** 9.931 dòng JSON (10 unit × 8 tiết: mục tiêu, media,
sections) chuyển sang `Global_Success_10/_source/data/`, kèm ba ảnh nháp ở
`_source/images/`. Thư mục bắt đầu bằng `_` nên Jekyll không xuất ra trang; đây
là **kho nội dung để dựng 79 tiết còn lại**, không phải tệp phục vụ.

Đừng nạp `_source/` bằng `fetch` — soạn tiết mới thì đọc JSON đó rồi viết thẳng
vào HTML, theo đúng §2.4.

---

## 1. Module này là gì

Bài giảng HTML để **giáo viên trình chiếu trên lớp**, thay cho bộ PowerPoint.
Giáo viên mở bản đồ, bấm vào ghim của unit, bài giảng hiện lên ngay tại chỗ.

Không phải học liệu tự học của học sinh. **Không nằm trong phạm vi đo lường của
luận văn** — artefact được đo là Practice Center + AI Error Log + xương sống log.
Module này chỉ đặt nhờ trên cùng tên miền cho tiện.

Hệ quả trực tiếp: **module này không sinh một dòng dữ liệu nghiên cứu nào**, nên
mọi cơ chế phục vụ đo lường đều không có mặt ở đây.

---

## 2. Bốn luật cứng

### 2.1 Không sinh log

Không `Spine.*`, không `import` bất cứ thứ gì trong `core/`, không `localStorage`,
không `fetch` tới `ENDPOINT`.

`Global_Success_10` **không có trong danh sách `module` đóng** của
`SITE-SPEC.md` §8.1 và không bao giờ được thêm vào. Một dòng log mang mã giáo
viên nằm lẫn giữa dữ liệu học sinh là thứ không ai nhớ lọc lúc phân tích.

Trạng thái slide chỉ nằm trong bộ nhớ. F5 là về đầu. **Đó là chủ ý, đừng "sửa"
thành lưu tiến độ.**

### 2.2 Không cổng khoá

SITE-SPEC §9.2 đặc tả một cổng mềm (`gs10-gate.js`, mật khẩu giáo viên, màn hình
khoá cho học sinh). **Đã bỏ, đừng dựng lại.**

Lý do cổng tồn tại trong bản cũ là để học sinh trong mẫu không tự học được bài
giảng — tức là một lo ngại về **thiết kế nghiên cứu**. Chủ dự án đã chốt module
này không phải đối tượng nghiên cứu, nên lo ngại đó không còn ràng buộc.

Đã có `<meta name="robots" content="noindex, nofollow">` ở mọi trang của module.
Giữ nguyên. Đừng thêm `Global_Success_10` vào sitemap hay thanh điều hướng.

### 2.3 Không banner AI

Nội dung bài giảng **do giáo viên viết**. Câu banner bắt buộc của `AGENTS.md` §6 —
*"All study materials are generated by AI and may contain inaccuracies"* — dán lên
đây là **nói sai sự thật**, và làm nhẹ đi cảnh báo ở chỗ nó đúng (Practice Center,
Xưởng AI).

> Ghi chú xuất xứ, để không ai kết luận nhầm lần nữa: các tệp
> `HK1_Grade10/*.docx` có `dc:creator: python-docx` **không phải** vì AI viết
> giáo án, mà vì chủ dự án dùng AI thêm mã năng lực theo khung của trường rồi
> xuất lại tệp. Nội dung là của giáo viên.

**Ngoại lệ duy nhất:** nếu về sau có mục nào do AI sinh thật, mục đó phải mang
chip 🤖 ngay tại chỗ theo `AGENTS.md` §6. Nhãn ở cấp item, không phải banner
toàn trang.

### 2.4 Chạy được khi mất mạng

Giáo viên mở bài trên máy phòng học, có thể không có Internet. Nhấp đúp
`index.html` trong Explorer phải chạy đúng, không cần server.

Bốn thứ **sẽ làm hỏng luật này** — đừng đưa vào:

| Cấm | Vì sao | Dùng gì thay |
|---|---|---|
| `fetch()` / `XMLHttpRequest` | Chrome chặn CORS trên `file://` → trang trắng | `<iframe src="...">` |
| `<script type="module">` | ES import bị chặn trên `file://` | `<script src="gs10.js">` thường |
| Google Fonts, CDN, ảnh từ URL ngoài | Không mạng là không có | font hệ thống, tệp trong `assets/` |
| `href="unit01/"` (đường dẫn thư mục) | `file://` ra danh sách tệp, không mở `index.html` | `href="unit01/index.html"` |

### 2.4a Ngoại lệ duy nhất: video YouTube nhúng

Chốt 2026-08-27. Clip khởi động của Unit 1 · Lesson 1 là video YouTube
(`gMlHi_R4y2A` — *Mr Bean doing housework*). **Đây là chỗ duy nhất trong module
cần Internet**, và nó phải giữ đúng ba điều kiện sau — bỏ điều nào cũng là phá
luật §2.4:

1. **Nạp chậm.** Slide chỉ có thẻ poster vẽ bằng CSS. `<iframe>` YouTube **chỉ
   được tạo khi cô bấm nút phát**. Đừng viết sẵn `<iframe>` vào HTML: làm vậy là
   mỗi lần mở slide đều gọi ra YouTube, chuyển slide bị khựng khi mạng yếu, và
   mất mạng thì để lại một ô đen giữa tiết.
2. **Mất mạng phải báo bằng chữ.** `initVideos()` kiểm `navigator.onLine`; nếu
   `false` thì đổi thẻ sang trạng thái `.offline` và chỉ xuống phương án dự
   phòng — hỏi thẳng cả lớp — ghi ngay dưới clip trên cùng slide đó. **Slide phải
   dạy được khi không có mạng.**
3. **Không tải ảnh nền từ YouTube.** Poster vẽ bằng gradient + chữ. Lấy
   `hqdefault.jpg` về làm nền thì mất mạng lại hỏng đúng chỗ vừa vá.

Thêm hai lựa chọn có lý do, giữ nguyên khi thêm video khác:

- `youtube-nocookie.com` thay vì `youtube.com` — chưa bấm phát thì không có
  cookie theo dõi nào. Trong phòng có học sinh, đây không phải chuyện nhỏ.
- `rel=0&modestbranding=1` — hết clip đỡ nhảy ra video lạ trước mặt cả lớp.
  **Không diệt hẳn được:** từ 2018 `rel=0` chỉ giới hạn gợi ý trong cùng kênh,
  chứ không tắt. Cách chắc ăn vẫn là dừng clip trước khi hết.

Markup:

```html
<div class="video" data-yt="gMlHi_R4y2A" data-title="Mr Bean doing housework">
  <button class="video-play" type="button" aria-label="Phát video">▶</button>
  <span class="t">Mr Bean doing housework</span>
  <span class="s">YouTube · bấm để phát · cần Internet</span>
</div>
```

Kiểm được: mở slide, DevTools → Network, **chưa bấm thì không có request nào ra
ngoài tên miền**. Đo ở bản hiện tại: 0 request trước khi bấm.

---

## 3. Kiến trúc

```
index.html  (bản đồ, KHÔNG BAO GIỜ reload)
     │
     │  bấm ghim → JS chặn click, mở overlay
     ▼
  .overlay > iframe  ─── src="unit01/index.html"   (BẢN ĐỒ UNIT, 8 thẻ tiết)
                            │
                            │  bấm thẻ tiết → iframe tự điều hướng bên trong
                            ▼
                       unit01/lesson1.html          (22 slide)

  Esc / ✕ → overlay đóng, iframe về about:blank (tắt audio), bản đồ nguyên vẹn
```

### Vì sao `<iframe>` chứ không `fetch` + `innerHTML`

Hai yêu cầu tưởng như đá nhau:

1. Bấm ghim mở bài **ngay trên bản đồ**, không reload (kiểu Duolingo).
2. **Mỗi bài là một website HTML độc lập** — mở thẳng vẫn đúng, sửa riêng không
   đụng bài khác, chép đi chỗ khác vẫn chạy.

`fetch` + `innerHTML` làm được vế 1 nhưng chết trên `file://` (§2.4). `<iframe>`
làm được cả hai, và giữ nguyên vế 2 vì mỗi bài vẫn là một tài liệu HTML đầy đủ.

**Đừng tối ưu chỗ này thành SPA.** Đây là quyết định có chủ đích, không phải nợ
kỹ thuật.

### Hai tầng bản đồ, một tầng popup

Bản đồ chính (`index.html`) và bản đồ unit (`unitNN/index.html`) dùng **chung cơ
chế `.pin`** — cùng CSS, cùng cách đo toạ độ. Khác một điểm: thẻ tiết dùng thêm
`.rect` để bo góc vuông hơn, vì thẻ tiết là hình chữ nhật chứ không phải viên
thuốc như ghim ở bản đồ chính.

**Chỉ tầng ngoài cùng mới mở popup.** `initOverlay()` thoát ngay nếu trang đang
nằm trong iframe:

```js
if (EMBEDDED) return;
```

Thiếu dòng đó thì bấm một tiết trên bản đồ unit sẽ mở **popup lồng trong popup** —
hai lớp nền tối, hai nút đóng, và `Esc` không biết đóng lớp nào. Bản đồ unit nằm
trong popup nên link tiết chỉ được đi tiếp bên trong chính iframe đó.

Mở thẳng `unitNN/index.html` (không qua popup) thì nó lại là tầng ngoài cùng, và
bấm tiết sẽ mở popup — đúng như mong đợi, không phải lỗi.

### Giao tiếp giữa iframe và trang cha

Trang con **không** được cha đọc DOM (trên `file://` trình duyệt chặn truy cập
chéo). Liên lạc duy nhất là `postMessage`:

```js
window.parent.postMessage({ gs10: 'close' }, '*');   // con xin đóng
```

Trang cha nghe `message`, thấy `gs10 === 'close'` thì đóng overlay. Chỉ có một
loại tin nhắn. Đừng phát sinh thêm giao thức.

`data-back` trên link ở thanh trên quyết định `Esc` làm gì:

| Giá trị | Ở trang | `Esc` làm gì khi đang trong popup |
|---|---|---|
| `data-back="map"` | mục lục unit | đóng hẳn popup |
| `data-back="unit"` | trang bài | lùi về mục lục, popup vẫn mở |

---

## 4. Kiểm kê tệp

```
Global_Success_10/
├── index.html          ✅ bản đồ 14 ghim
├── gs10.css            ✅ CSS duy nhất của module
├── gs10.js             ✅ popup + slide + lật đáp án
├── README.md           ✅ hướng dẫn viết bài (cho người soạn, không phải deploy)
├── assets/
│   ├── map.webp           ✅ bản đồ chính
│   ├── unit01-map.webp … unit10-map.webp  ✅ bản đồ Unit 1–10
│                          (11 ảnh, mỗi ảnh 1536×1024, tổng 4,2 MB)
│   └── audio/
│       └── u01-track02.mp3 ✅ Task 1 · 1:43 · 830 KB
│                           ❌ các track khác chép khi dựng tới bài cần
├── unit01/
│   ├── index.html      ✅ BẢN ĐỒ UNIT — 8 thẻ, cả 8 đã mở
│   └── lesson1–8.html  ✅ 8 bài giảng, theme "Family Time"
└── unit02/
    ├── index.html      ✅ BẢN ĐỒ UNIT — 8 thẻ, cả 8 đã mở
    └── lesson1–8.html  ✅ 8 bài giảng, theme "Pollution"
    ├── lesson1.html    ✅ ĐẦY ĐỦ — 21 slide, bám giáo án + SGK tr. 8–9
    └── lesson2–8.html  ❌ chưa dựng
├── unit02/ … unit10/
│   └── index.html      ✅ BẢN ĐỒ UNIT — 8 thẻ mỗi unit, tất cả còn khoá
├── tools/
│   └── measure-map.py  ✅ script đo toạ độ thẻ (chỉ dùng lúc dựng, không phải build)
├── _source/            ✅ kho nội dung, KHÔNG phục vụ ra trang — xem §0.1
│   ├── data/           9.931 dòng JSON: 10 unit × 8 tiết
│   └── images/         ba ảnh nháp của bản cũ
review1–4               ❌ chưa có bản đồ, ghim vẫn mờ trên bản đồ chính
review1–4.html          ❌ chưa dựng
```

Trên bản đồ, ghim chưa có bài là `<span class="pin" aria-disabled="true">` — hiện
mờ, không bấm được. Ghim đã có bài là `<a class="pin" href="…">`.

### Toạ độ vùng bấm — đo bằng máy, đừng chỉnh tay

Mỗi thẻ mang bốn biến trong `style`: `--x` `--y` là **góc trên–trái**, `--w` `--h`
là kích thước, tất cả tính theo **% của ảnh**. Khi mở khoá một unit, đổi `<span>`
thành `<a href>` và **giữ nguyên cả bốn** — chúng đã ôm sát giọt ghim và thẻ tên
vẽ sẵn trong ảnh.

Bốn số này không ước lượng bằng mắt mà **đo bằng máy**.

**Bản đồ unit** (`unitNN/index.html`) — chạy script có sẵn:

```bash
python tools/measure-map.py assets/unit02-map.webp --check /tmp/check.png
```

In ra 8 dòng `style="--x:… --y:… --w:… --h:…"` dán thẳng vào HTML.
`--check` xuất ảnh có vẽ khung hồng — **luôn soi ảnh này trước khi dán.**

Script neo vào huy hiệu số (hình tròn đậm màu ở góc trên–trái mỗi thẻ), rồi lan
vùng sáng của thân thẻ trong một cửa sổ chặn quanh huy hiệu. Vật tròn cùng màu
trong tranh (máy tính bảng, vòi tưới) bị loại vì không có thân thẻ đi kèm — ở
Unit 2 script tự bỏ đúng 2 vật như vậy.

**Mặt nạ không khoá theo màu.** Bản đầu dò huy hiệu theo màu (xanh lục cho Unit
1–2, tím cho Unit 3) và hỏng ngay ở Unit 4 — bản đồ đó dùng huy hiệu nhiều màu
trong cùng một trang: 06 đỏ, 01 xanh lam, còn lại xanh lục. Hiện tại huy hiệu là
"hình tròn đậm bất kỳ màu gì", thân thẻ là "mảng sáng ít bão hoà".

**Ba chế độ, dùng theo thiết kế bản đồ:**

| Chế độ | Dùng khi | Đã chạy cho |
|---|---|---|
| tự động (mặc định) | huy hiệu đè góc trên–trái thân thẻ | Unit 1, 2, 3, 4 |
| `--seeds "x,y; …"` | dò tự động không ra đủ 8; người chỉ 8 điểm trong thân thẻ, máy đo mép | Unit 5, 6, 10 |
| đo tay theo hàng | 8 thẻ xếp liền một hàng, khe giữa hai thẻ chỉ ~15px nên phép quét nối chúng thành một hộp dài | Unit 7, 8, 9 |

Bản đồ Unit 10 dùng **giọt ghim rời nằm phía trên thẻ** chứ không đè góc; giọt
nước trượt bộ lọc tròn nên script có nhánh dự phòng lấy mảng đậm trong dải 72px
ngay trên thẻ. Dải hẹp là cố ý — nới rộng là vơ luôn tóc nhân vật.

**Cắt đuôi chỉ dẫn — chỉ bật cho tông có đuôi.** Bản đồ Unit 3 nối mỗi thẻ xuống
nhân vật bằng một nét cong trắng cùng màu thân thẻ; không cắt thì hộp cao gấp ba
lần thẻ thật. Cờ `trim` trong `THEMES` bật riêng cho tông tím. **Đừng bật cho
tông xanh lục** — đã thử: thẻ hai dòng của Unit 2 bị loại (còn 6/8) và toạ độ
Unit 1 lệch vài px.

Thẻ nào bị nhân vật che thì vùng lan có thể rò hoặc cụt; ở Unit 1 hai thẻ
**06 Writing** và **07 Communication & Culture** phải đo tay bằng cách quét từng
hàng ngang. Soi `--check` là thấy ngay.

**Bản đồ chính** (`map.webp`) đo bằng cách khác, vì thẻ tên ở đó là **xanh lục
đậm** chứ không phải kem: lọc `G` 60–175, `R < 110`, `B < 130`, `G-R > 28`,
`G-B > 18`; giữ vùng cao 35–45px, đặc ≥ 70% (loại tán cây và biển START cùng
màu); nới 26px trái · 10px trên · 6px dưới · 3px phải cho vừa giọt ghim.

Kết quả đo ở bản hiện tại: bản đồ chính 14 ô, bản đồ Unit 1–10 mỗi bản 8 thẻ
(88 vùng bấm). Kiểm trong trình duyệt: **không ô nào chồng nhau, không ô nào
tràn khỏi ảnh** ở cả 10 unit.

> Toạ độ hai thẻ **06 Writing** và **07 Communication & Culture** của Unit 1 là
> đo tay, không phải kết quả script — hai thẻ đó bị con nhím và con gấu mèo che
> nên vùng lan rò sang sách vở của chúng. Chạy lại script trên `unit01-map.webp`
> sẽ ra số khác cho đúng hai thẻ này; **đừng thay số đang chạy bằng số script.**

**Nếu thay ảnh bản đồ:** đo lại toàn bộ, đừng sửa lẻ từng số. Vùng bấm to hơn
thẻ tên trông như lỗi hiển thị — đó chính là lỗi đã phải sửa một lần rồi.

---

## 5. Deploy

Repo: `https://github.com/hsg2627/hsg2627.github.io` · nhánh `main` · GitHub Pages
user site, phục vụ thẳng từ gốc nhánh.

```bash
git add Global_Success_10 GLOBAL-SUCCESS-10.md
git commit -m "Add Global Success 10 lecture module (teacher projection, no telemetry)"
git push origin main
```

Không cần bước build, không có bundler, không có phụ thuộc npm. Tệp đẩy lên sao
thì phục vụ y vậy.

Sau khi Pages dựng xong, module ở `https://hsg2627.github.io/Global_Success_10/`.

**Ba điều cần biết về GitHub Pages ở đây:**

- Repo là **user site** nên trang chạy ở gốc tên miền. Đường dẫn tương đối trong
  module (`../gs10.css`, `unit01/index.html`) đúng ở cả `file://` lẫn web.
  **Đừng đổi sang đường dẫn tuyệt đối `/`** — đổi là hỏng `file://`.
- **Ảnh bản đồ lưu ở định dạng WebP.** 11 ảnh PNG gốc nặng 30,1 MB; nén WebP
  chất lượng 88 còn **4,2 MB — giảm 86%**, và ở mức phóng 3× thì chữ trên thẻ
  không phân biệt được với bản gốc. Đừng đưa PNG trở lại.

  Chỉ số PSNR toàn ảnh chỉ 32–36 dB nghe như thấp, nhưng đó là **lệch màu ở nền
  rậm**, không phải chữ nhoè — tăng chất lượng lên 95 cũng chỉ thêm ~2 dB mà
  nặng thêm 75%. Đã soi vùng chữ ở mức phóng 3× trước khi chốt.

  Thêm bản đồ mới thì nén trước khi commit:

  ```python
  from PIL import Image
  Image.open('unitNN-map.png').convert('RGB').save(
      'unitNN-map.webp', 'WEBP', quality=88, method=6)
  ```

  **Toạ độ vùng bấm tính theo %, nên nén ảnh KHÔNG phải đo lại gì cả** — miễn là
  giữ nguyên kích thước 1536×1024.

- WebP chạy trên Chrome 32+, Edge 18+, Firefox 65+, Safari 14+. Máy nào cũ hơn
  thì cũng đã không chạy được module này rồi (CSS biến `--x/--y` cần cùng đời
  trình duyệt), nên không cần ảnh dự phòng.
- Pages **không** phục vụ `index.html` cho đường dẫn thư mục con một cách đảm bảo
  giống mọi máy chủ. Cứ trỏ thẳng `index.html` như §2.4 quy định là đúng cả hai nơi.

---

## 6. Kiểm tra sau khi deploy

> ⚠️ **Đây là ngoại lệ của `AGENTS.md` §11.**
>
> `AGENTS.md` §11 cấm tác nhân bấm thử trang sau khi deploy, vì xương sống ghi
> log mọi thao tác — kể cả thao tác của máy — và làm bẩn dữ liệu nghiên cứu.
>
> **Luật đó không áp cho `Global_Success_10/`,** vì module này không có xương
> sống và không sinh sự kiện nào (§2.1). Bấm thử ở đây là an toàn và **nên làm**.
>
> Nhưng chỉ trong thư mục này. Đi lạc sang `/practice/`, `/ai-logs/`, `/me/` hay
> trang chủ thì §11 áp trở lại ngay: **không bấm, không nhập mã, không chạy thử.**

Kiểm bảy mục, theo thứ tự:

- [ ] Mở `/Global_Success_10/` → bản đồ hiện đủ ảnh, 14 ghim
- [ ] Ghim Unit 1–5 bấm được; Unit 6–10 và Review 1–4 hiện mờ, bấm không ăn
- [ ] Bấm Unit 1 → popup mở, **URL trên thanh địa chỉ không đổi**
- [ ] Trong popup bấm Lesson 1 → ra 8 slide, `← →` chuyển được, đếm `1 / 8`
- [ ] Bấm "Đáp án" ở một câu → chỉ câu đó hiện, các câu khác vẫn ẩn
- [ ] `Esc` → lùi về mục lục; `Esc` lần nữa → đóng popup, bản đồ nguyên chỗ cũ
- [ ] **Không slide nào phải cuộn** ở 1366×768. Đo bằng đoạn này trong Console:

      document.querySelectorAll('.slide').forEach((s,i)=>{ s.classList.add('is-current');
        const o=s.scrollHeight-s.clientHeight; if(o>4) console.log('slide',i+1,'tràn',o,'px');
        if(i) s.classList.remove('is-current'); });

- [ ] **Kiểm không có log:** mở DevTools → Network, bấm khắp module. Không được
      có request nào đi ra ngoài tên miền, đặc biệt là tới Apps Script.

Mục cuối là mục quan trọng nhất. Có một request đi ra là luật §2.1 đã vỡ ở đâu đó.

**Kiểm ngoại tuyến** (chỉ làm được trên máy, không làm được từ trang đã deploy):
tải thư mục về, ngắt mạng, nhấp đúp `index.html`. Bản đồ và bài giảng phải chạy
đủ. Đây là phép kiểm duy nhất chứng minh được §2.4.

---

## 7. Thêm bài, thêm unit

### Thêm một tiết

1. Chép `unit01/lesson1.html` → `lesson2.html`.
2. Sửa `<title>`, `.bar .brand`, và các `<section class="slide">`. Thêm bớt slide
   tuỳ ý — **JS tự đếm lại**, không khai số slide ở đâu cả.
3. Trong `unit01/index.html`, xoá `aria-disabled="true" onclick="return false"`
   khỏi thẻ của tiết đó.

### Thêm một unit

1. Tạo `unitNN/`, chép `unit01/index.html`, sửa danh sách 8 tiết.
2. Trong `index.html` gốc: đổi `<span class="pin" aria-disabled="true" …>` của
   unit đó thành `<a class="pin" href="unitNN/index.html" …>`, **giữ nguyên
   `--x/--y/--w`**.

### Thêm tệp nghe

Nguồn nằm ngoài repo, ở máy chủ dự án:
`Documents\GA Global Success 10 NLS-…\Audio\Unit N\TrackNN.mp3` — đủ 14 thư mục
(Unit 1–10, Review 1–4), Unit 1 có 10 track.

**Chỉ chép track nào bài đang dùng.** Chép cả bộ là thêm vài chục MB vào repo cho
những bài chưa dựng.

Quy ước tên: `assets/audio/uNN-trackMM.mp3` — **giữ đúng số track của sách giáo
khoa**, đừng đánh số lại theo tiết. Cô đối chiếu với sách nhanh hơn, và tránh
việc một track dùng ở hai tiết thì phải chép hai lần dưới hai tên khác nhau.

```bash
cp "…/Audio/Unit 1/Track03.mp3" Global_Success_10/assets/audio/u01-track03.mp3
```

Trong slide, ghi thời lượng vào huy hiệu ở hàng đầu để cô liệu giờ, và dùng
`preload="metadata"` (không phải `none`) để thanh tua hiện sẵn thời lượng:

```html
<span class="badge soft">🎧 Track 02 · 1:43 · nghe 2 lượt</span>
…
<audio class="tight" controls preload="metadata" src="../assets/audio/u01-track02.mp3">
```

`class="tight"` thu lề thanh audio; thiếu nó là slide hội thoại tràn màn 768px.

### Bộ khung viết slide

| Dùng để | Markup |
|---|---|
| Bìa bài (nền gradient màu unit) | `<section class="slide cover">` + `<span class="unit-no">1</span>` |
| Slide phân đoạn (Warm-up, Practice…) | `<section class="slide phase">` + `<span class="step">2</span>` |
| Nhãn nhỏ trên tiêu đề | `<span class="kicker">` |
| Huy hiệu thời lượng / mã năng lực | `<span class="badge time">5 min</span>` · `<span class="badge code">10.C2.3</span>` |
| Lưới thẻ (mục tiêu, luật chơi) | `<ul class="cards"><li class="card">` (thêm `alt` để đổi màu viền) |
| Thẻ từ vựng | `<ul class="words"><li class="word q">` |
| Danh sách chip | `<ul class="chips"><li class="hot">` |
| Hội thoại | `<div class="chat"><div class="bubble l\|r"><span class="who">Nam</span>…` |
| Đúng/Sai | `<ul class="tf"><li class="q">` + `<span class="answer verdict t\|f">` |
| Ghép động từ – danh từ | `<ul class="colloc"><li class="q"><span class="v">…</span><span class="n">…</span>` |
| So sánh hai cột (ngữ pháp) | `<div class="compare"><div>…</div><div>…</div></div>` |
| Khung nhiệm vụ lớn | `<div class="brief">` + `<p class="slogan">` |
| Khung nhấn mạnh nhỏ | `<div class="box">` |
| Hai cột thường | `<div class="cols">` |
| Câu hỏi có đáp án lật | `<p class="q">… <button class="reveal" type="button" aria-expanded="false">Đáp án</button> <span class="answer">…</span></p>` |
| Giải thích dài, xuống dòng | thêm `note`: `<span class="answer note">` |

Slide nào có `.answer` thì JS tự thêm nút **Hiện hết đáp án** ở góc dưới.

**Ba cái bẫy khi viết slide:**

1. **Mỗi chỗ trống một `.q` riêng.** Nút lật mở mọi `.answer` trong `.q` gần
   nhất. Một câu có hai chỗ trống mà chỉ bọc một `.q` thì bấm một nút lật cả hai.
2. **Đừng nhét `<ul>`/`<div>` vào `<span class="answer">`.** Trình duyệt đẩy nó
   ra ngoài `<p>` và nút lật mất tác dụng. Dùng `<div class="answer note">`.
3. **Slide phải vừa màn 1366×768, không cuộn.** Nhiều chữ thì thêm
   `class="slide dense"` (0,82×) hoặc `denser` (0,72×); vẫn tràn thì **tách slide**,
   đừng thu chữ nhỏ thêm — hàng ghế cuối không đọc được.

### Màu

Bảng màu gốc (`AGENTS.md` §12) giữ nguyên làm nền, chữ, đúng/sai. **Có bổ sung**
một cặp màu nhấn cho mỗi unit, lấy theo màu unit trong sách giáo khoa để slide
chiếu lên khớp trang sách học sinh đang mở — Unit 1 là cam `#E1541C` / xanh mòng
két `#0E7C86`, theo trang 8–9.

Khai bằng `<body data-unit="N">`; các biến `--accent`, `--accent-2`,
`--accent-soft` tự đổi theo. **Đừng đặt mã hex thẳng vào slide** — thêm unit mới
thì thêm một dòng `body[data-unit="N"]` trong `gs10.css`, và nhớ kiểm tương phản
khi chiếu lên tường, không chỉ nhìn trên laptop.

---

## 8. Phím dùng trong lớp

`←` `→` chuyển slide · `F` toàn màn hình · `A` hiện hết đáp án của slide hiện tại ·
`Esc` lùi một bậc · `Ctrl+P` in ra giấy (bản in hiện sẵn mọi đáp án, ẩn thanh
điều hướng).

---

## 9. Nhật ký quyết định

| Ngày | Quyết định | Vì sao |
|---|---|---|
| 2026-08-26 | GS10 là bài giảng của giáo viên, ngoài phạm vi nghiên cứu | Công cụ dạy học đặt nhờ trên cùng tên miền |
| 2026-08-27 | **Bỏ cổng khoá** | Không phải đối tượng nghiên cứu thì lập luận "can thiệp thứ hai" không còn ràng buộc |
| 2026-08-27 | **Bỏ banner AI** | Nội dung do giáo viên viết; dán banner là nói sai sự thật và làm nhẹ cảnh báo ở chỗ nó đúng |
| 2026-08-27 | **Không lưu tiến độ** | Cô đứng máy chiếu không có "tiến độ"; có tiến độ nghĩa là có HS đang tự học ở đây |
| 2026-08-27 | **Popup bằng `<iframe>`, không `fetch`** | `fetch` chết trên `file://`; iframe giữ được cả "không reload" lẫn "mỗi bài một tệp độc lập" |
| 2026-08-27 | Nội dung bắt đầu từ HK1, Unit 1–5 | Mới có 5 giáo án; bài giảng do chủ dự án tự viết HTML, giáo án chỉ để đối chiếu |

---

## 10. Khi không chắc

Dừng lại và hỏi chủ dự án. Đặc biệt với ba việc sau — cả ba đều trông như "sửa
cho đúng chuẩn" nhưng thực chất là phá bỏ một quyết định có chủ đích:

1. Thêm cổng khoá, mật khẩu, hay bất kỳ cơ chế phân vai nào.
2. Thêm banner AI, hoặc thêm bản dịch tiếng Việt dưới banner ở bất kỳ trang nào
   của trang web (`SITE-SPEC.md` §0.4b — đề xuất này đã bị bác một lần).
3. Thêm log, tiến độ, XP, huy hiệu, hay bất kỳ thứ gì ghi lại thao tác người dùng
   trong module này.
